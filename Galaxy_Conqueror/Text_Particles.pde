class Text_Particles { //This class is about the score's text display when an enemy gets hit or touches the bottom of the screen. It was written by Dylan Kleton
  
  void drawText (float textX, float textY)
  {
   
      fill(255, 0, 0);
      textSize(55);
      text("-100", textX, textY);
    
  }
}
